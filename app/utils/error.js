export const missingParameterError = (error) => {
  return `Please enter ${error}`;
};

export const invalidParameterError = (error) => {
  return `Invalid  ${error}`;
};
export const missingImageError = (error) => {
  return `Please upload an image ${error}`;
};
